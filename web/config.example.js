// Optional frontend runtime config for hosted static deployments.
// Copy to web/config.js and set your public backend URL.
// Example:
// window.API_BASE_URL = "https://your-tunnel.trycloudflare.com";

window.API_BASE_URL = "";

// Optional API key used when BACKEND_API_KEY is set on the server.
// You can also pass it at runtime via ?api_key=... and it will be persisted.
window.API_AUTH_KEY = "";
